DROP TABLE IF EXISTS orgs;
CREATE TABLE orgs (
  id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  org_name varchar NOT NULL,
  admin int REFERENCES users(id) ON DELETE CASCADE,
  part_of INT REFERENCES orgs(id)
);
ALTER TABLE users
  ADD COLUMN 
    org int REFERENCES users(id) ON DELETE SET NULL;